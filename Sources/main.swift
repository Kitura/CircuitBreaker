import Foundation

// TODO: Decide if these methods are necessary and update

/*
public func createBreaker (timeout: Double = 10, resetTimeout: Double = 60, maxFailures: Int = 5, selector: @escaping (Any...) -> (Any)) -> CircuitBreaker {
   
    let newBreaker = CircuitBreaker(timeout: timeout, resetTimeout: resetTimeout, maxFailures: maxFailures, selector: selector)
    
    return newBreaker
    
}

public func createStats () -> Stats {
    let newStats = Stats()
    
    return newStats
}
*/

print("Done")
